# HOL 3 - Score Calculator App (Function Component + CSS)

## Objectives
- Create a function component
- Apply styling via a separate CSS file
- Pass props (Name, School, total, goal) and compute average score

Note: original lab math is total/goal (goal used as the divisor per the HOL doc),
formatted as a percentage string.

## How to run
npm install
npm start
